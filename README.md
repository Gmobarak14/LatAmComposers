Here is the link if you are not trying to run the site locally: https://gmobarak14.github.io/LatAmComposers/
for some reason, the audio embedded into the composer nodes is not working on the githup pages hosted site which is strange because the background music is stored in the same folder and it works fine. 
However, the composer node audio works fine locally as shown in the video. 
